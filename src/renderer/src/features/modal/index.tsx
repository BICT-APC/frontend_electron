import { useModalState } from '../../features/modal/lib/use-modal-state'
import { ModalType } from '../../features/modal/model/types'
import { Modal } from './ui/modal'

export { useModalState, ModalType, Modal }
